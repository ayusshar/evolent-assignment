import React from 'react';
import { HEADER_TEXT } from '../constants';
export const Header = () => {
    return (
        <div className="header">
            <p>{HEADER_TEXT}</p>
        </div>
    )
}